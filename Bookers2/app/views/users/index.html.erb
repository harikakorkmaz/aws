<div class='body'>
  <p id="notice"></p>
  <div class='container'>
    <div class='row'>
      <div class='col-lg-3'>
        <%= render 'list' %>
        <%= render 'form' %>
      </div>
      <div class='col-lg-9'>
        <h2>Users</h2>
        <table class='table'>
          <thead>
            <tr>
            <th>image</th>
            <th>name</th>
            <th></th>
            <th colspan="3"></th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <!-- <%= @users.each do |user| %> -->
                <td><=%= attachment%></td>
                <td><=%= user.name %></td>
                <td><=%= link_to "show", show_user_path(user) %></td>
              <!-- <% end %> -->
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
